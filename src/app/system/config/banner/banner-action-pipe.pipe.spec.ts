import { BannerActionPipePipe } from './banner-action-pipe.pipe';

describe('BannerActionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BannerActionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
