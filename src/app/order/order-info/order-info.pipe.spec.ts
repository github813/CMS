import { OrderInfoPipe } from './order-info.pipe';

describe('OrderInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
