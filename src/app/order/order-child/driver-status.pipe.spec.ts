import { DriverStatusPipe } from './driver-status.pipe';

describe('DriverStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DriverStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
