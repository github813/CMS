import { OrderChildStatusPipe } from './order-child-status.pipe';

describe('OrderChildStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderChildStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
