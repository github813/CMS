import { OrderChildLogTypePipe } from './order-child-log-type.pipe';

describe('OrderChildLogTypePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderChildLogTypePipe();
    expect(pipe).toBeTruthy();
  });
});
